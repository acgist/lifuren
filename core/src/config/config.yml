# HTTP监听
http-server:
  host: 0.0.0.0
  port: 8080
# 图片页面配置
image:
  client: stable-diffusion-cpp
  output: /data/image
  clients:
    - stable-diffusion-cpp
    - paint-cycle-gan
    - paint-style-gan
# 诗词页面配置
poetry:
  client: poetize-rnn
  clients:
    - poetize-rnn
# 诗词标记页面配置
poetry-mark:
  - path: /data/mark/poetry
# RAG
rag:
  id: 0
  # elasticsearch
  type: elasticsearch
  size: 4
  # ollama|chinese-word-vectors
  embedding: ollama
# Embedding
embedding:
  # ollama|chinese-word-vectors
  type: ollama
# Ollama配置
# https://github.com/ollama/ollama/blob/main/docs/api.md
ollama:
  api: http://localhost:11434
  username: 
  password: 
  # NONE|Basic|Token
  auth-type: NONE
  embedding:
    path: /api/embed
    model: quentinz/bge-large-zh-v1.5
    options: {}
# ElasticSearch配置
elasticsearch:
  api: http://localhost:9200
  username: elastic
  password: elastic
  # NONE|Basic|Token
  auth-type: Basic
# chinese-word-vectors配置
chinese-word-vectors:
  path: /data/Chinese-Word-Vectors/sgns.sikuquanshu.word
# poetize-rnn配置
poetize-rnn:
  model: /data/model/poetry-rnn.model
# paint-cycle-gan配置
paint-cycle-gan:
  model: /data/model/paint-cycle-gan.model
# paint-style-gan配置
paint-style-gan:
  model: /data/model/paint-style-gan.model
# stable-diffusion-cpp配置
stable-diffusion-cpp:
  model: /data/stable-diffusion-cpp/model.ckpt
  # https://github.com/leejet/stable-diffusion.cpp/blob/master/examples/cli/main.cpp
  options: {}
  
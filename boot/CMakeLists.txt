add_subdirectory(main)

if(CMAKE_BUILD_TYPE MATCHES "Debug")
    if(ENABLE_TEST)
        message(STATUS "编译测试代码")
        add_definitions(-DLFR_ENABLE_TEST=true)
        add_subdirectory(test)
    else()
        message(STATUS "忽略编译测试代码")
    endif()
else()
    message(STATUS "忽略编译测试代码")
endif()
